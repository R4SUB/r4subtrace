% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_model.R
\name{build_trace_model}
\alias{build_trace_model}
\title{Build a Trace Model}
\usage{
build_trace_model(
  adam_meta,
  sdtm_meta,
  mapping = NULL,
  spec = NULL,
  config = trace_config_default()
)
}
\arguments{
\item{adam_meta}{A data.frame of ADaM variable metadata. Must contain
\code{dataset} and \code{variable} columns.}

\item{sdtm_meta}{A data.frame of SDTM variable metadata. Must contain
\code{dataset} and \code{variable} columns.}

\item{mapping}{An optional data.frame describing ADaM-to-SDTM mappings.
Must contain \code{adam_dataset}, \code{adam_var}, \code{sdtm_domain}, \code{sdtm_var}.}

\item{spec}{Reserved for future use (ADaM spec ingestion).}

\item{config}{A \code{trace_config} object from \code{\link[=trace_config_default]{trace_config_default()}}.}
}
\value{
A list of class \code{"trace_model"} with elements:
\itemize{
\item \code{nodes}: tibble of asset nodes (datasets and variables)
\item \code{edges}: tibble of relationships between nodes
\item \code{diagnostics}: list of tibbles (\code{orphans}, \code{ambiguities}, \code{conflicts})
\item \code{config}: the configuration used
}
}
\description{
Constructs a directed trace model (nodes + edges + diagnostics) from
ADaM metadata, SDTM metadata, and an optional mapping sheet.
}
\examples{
adam_meta <- data.frame(
  dataset = "ADSL", variable = c("STUDYID", "USUBJID", "AGE"),
  label = c("Study ID", "Unique Subject ID", "Age")
)
sdtm_meta <- data.frame(
  dataset = "DM", variable = c("STUDYID", "USUBJID", "AGE"),
  label = c("Study ID", "Unique Subject ID", "Age")
)
map <- data.frame(
  adam_dataset = "ADSL", adam_var = c("STUDYID", "USUBJID", "AGE"),
  sdtm_domain = "DM",   sdtm_var = c("STUDYID", "USUBJID", "AGE")
)
tm <- build_trace_model(adam_meta, sdtm_meta, mapping = map)
tm$nodes
tm$edges

}
