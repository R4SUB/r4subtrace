% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_impact.R
\name{trace_impact}
\alias{trace_impact}
\title{Impact Analysis for a Changed Source Variable}
\usage{
trace_impact(trace_model, changed, adam_derivations = NULL, max_depth = Inf)
}
\arguments{
\item{trace_model}{A \code{trace_model} from \code{\link[=build_trace_model]{build_trace_model()}}.}

\item{changed}{A single variable identifier as \code{"DATASET.VARIABLE"}, for
example \code{"DM.AGE"} or \code{"ADSL.AGE"}.}

\item{adam_derivations}{Optional data.frame of ADaM-to-ADaM derivations with
columns \code{from_dataset}, \code{from_var}, \code{to_dataset}, \code{to_var}, as in
\code{\link[=trace_chains]{trace_chains()}}.}

\item{max_depth}{Optional maximum number of hops to report. \code{Inf} (the
default) reports the full transitive set.}
}
\value{
A tibble with one row per impacted variable: \code{dataset}, \code{variable},
\code{role}, \code{depth} (hops from the change), and \code{path} (a readable derivation
path from the impacted variable back to the changed one). Rows are ordered
nearest first. Zero rows means nothing derives from the variable. The
result carries a \code{"changed"} attribute with the resolved identifier.
}
\description{
Answers the question a data team asks before touching a source variable: if
this changes, what downstream ADaM variables are affected? Given a trace
model and a variable that is about to change, \code{trace_impact()} follows the
derivation graph in reverse and returns every variable that derives from it,
directly or through a chain.
}
\details{
The trace model records derivations as edges from a derived variable to its
source. Impact analysis walks those edges backwards from the changed variable
to find its dependents. ADaM-to-ADaM derivations are included when supplied
through \code{adam_derivations}, exactly as in \code{\link[=trace_chains]{trace_chains()}}, so multi-step
impact (an \code{ADTTE} variable built from an \code{ADSL} variable built from \code{DM}) is
captured. Each dependent is reported once, at its shortest distance from the
change, with a readable path back to it. Circular references are handled and
do not loop.
}
\examples{
adam <- data.frame(
  dataset = c("ADSL", "ADAE"), variable = c("AGE", "AAGE"),
  label = c("Age", "Analysis Age")
)
sdtm <- data.frame(dataset = "DM", variable = "AGE", label = "Age")
map <- data.frame(
  adam_dataset = "ADSL", adam_var = "AGE",
  sdtm_domain = "DM", sdtm_var = "AGE", confidence = 1
)
tm <- build_trace_model(adam, sdtm, mapping = map)
deriv <- data.frame(
  from_dataset = "ADAE", from_var = "AAGE",
  to_dataset = "ADSL", to_var = "AGE"
)
# If DM.AGE changes, what is affected?
trace_impact(tm, "DM.AGE", adam_derivations = deriv)

}
