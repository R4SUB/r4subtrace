% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_export.R
\name{traceability_coverage}
\alias{traceability_coverage}
\title{Traceability Coverage by ADaM Dataset}
\usage{
traceability_coverage(trace_model, traced_min_level = 2L)
}
\arguments{
\item{trace_model}{A \code{trace_model} from \code{\link[=build_trace_model]{build_trace_model()}}.}

\item{traced_min_level}{The minimum trace level that counts as traced, an
integer in \code{0:3}. Default \code{2}.}
}
\value{
A tibble with one row per ADaM dataset: \code{adam_dataset},
\code{n_variables}, \code{n_traced}, \code{n_untraced}, \code{pct_traced}, and \code{untraced_vars}
(a comma-separated list, empty when the dataset is fully traced).
}
\description{
Summarizes how much of each ADaM dataset is traced back to SDTM. A variable
counts as traced when its trace level reaches \code{traced_min_level}. The default
of \code{2} treats an existing SDTM mapping (level L2) as traced.
}
\examples{
adam_meta <- data.frame(
  dataset = "ADSL", variable = c("STUDYID", "USUBJID", "AGEGR1"),
  label = c("Study ID", "Unique Subject ID", NA)
)
sdtm_meta <- data.frame(dataset = "DM", variable = c("STUDYID", "USUBJID"))
map <- data.frame(
  adam_dataset = "ADSL", adam_var = c("STUDYID", "USUBJID"),
  sdtm_domain = "DM", sdtm_var = c("STUDYID", "USUBJID"),
  confidence = c(1, 1)
)
tm <- build_trace_model(adam_meta, sdtm_meta, mapping = map)
traceability_coverage(tm)

}
