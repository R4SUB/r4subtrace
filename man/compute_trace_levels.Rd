% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_levels.R
\name{compute_trace_levels}
\alias{compute_trace_levels}
\title{Compute Trace Levels for ADaM Variables}
\usage{
compute_trace_levels(trace_model)
}
\arguments{
\item{trace_model}{A \code{trace_model} object from \code{\link[=build_trace_model]{build_trace_model()}}.}
}
\value{
A tibble with columns: \code{adam_dataset}, \code{adam_var}, \code{trace_level},
\code{has_mapping}, \code{has_derivation_text}, \code{n_candidates}, \code{max_confidence}.
}
\description{
Assigns a traceability level (L0--L3) to each ADaM variable in the trace
model based on available mapping, derivation text, and confidence scores.
}
\details{
Trace levels:
\itemize{
\item \strong{L0}: No mapping and no derivation text.
\item \strong{L1}: Derivation text present but no SDTM mapping.
\item \strong{L2}: Mapping to SDTM variable/domain exists.
\item \strong{L3}: Mapping exists AND (confidence >= threshold OR derivation text
present alongside mapping).
}
}
\examples{
adam_meta <- data.frame(
  dataset = "ADSL", variable = c("STUDYID", "USUBJID", "AGE", "AGEGR1"),
  label = c("Study ID", "Unique Subject ID", "Age", "Age Group")
)
sdtm_meta <- data.frame(
  dataset = "DM", variable = c("STUDYID", "USUBJID", "AGE"),
  label = c("Study ID", "Unique Subject ID", "Age")
)
map <- data.frame(
  adam_dataset = "ADSL", adam_var = c("STUDYID", "USUBJID", "AGE"),
  sdtm_domain = "DM",   sdtm_var = c("STUDYID", "USUBJID", "AGE"),
  confidence = c(1.0, 1.0, 0.9)
)
tm <- build_trace_model(adam_meta, sdtm_meta, mapping = map)
compute_trace_levels(tm)

}
