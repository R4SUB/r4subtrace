% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_chains.R
\name{trace_chains}
\alias{trace_chains}
\title{Follow Multi-Step Derivation Chains}
\usage{
trace_chains(trace_model, adam_derivations = NULL, trace_depth = 3L)
}
\arguments{
\item{trace_model}{A \code{trace_model} from \code{\link[=build_trace_model]{build_trace_model()}}.}

\item{adam_derivations}{Optional data.frame of ADaM-to-ADaM derivations with
columns \code{from_dataset}, \code{from_var}, \code{to_dataset}, \code{to_var}.}

\item{trace_depth}{Maximum number of hops to follow, a positive integer.
Default \code{3}.}
}
\value{
A tibble with one row per ADaM variable: \code{adam_dataset}, \code{adam_var},
\code{max_depth} (longest chain in hops), \code{grounded} (a path reaches SDTM),
\code{terminal_source}, \code{has_cycle}, \code{truncated} (a path hit \code{trace_depth}),
\code{n_paths}, and \code{longest_chain} (a readable path string).
}
\description{
Traces each ADaM variable back to its ultimate source through chains of any
depth, not just a single ADaM-to-SDTM hop. Real submissions often derive an
ADaM variable from another ADaM variable that is itself derived from SDTM
(for example an \code{ADAE} variable built from \code{ADSL}, which in turn comes from
\code{DM}). Shallow tracing misses those links and understates coverage.
}
\details{
The ADaM-to-SDTM links come from the trace model's edges. ADaM-to-ADaM links
are supplied through \code{adam_derivations}, a data.frame with columns
\code{from_dataset}, \code{from_var}, \code{to_dataset}, \code{to_var}, where \code{from} is the
derived variable and \code{to} is its source. Each ADaM variable is walked toward
its sources until the path reaches an SDTM variable (grounded), dead-ends at
an ADaM variable with no further source (ungrounded), revisits a node
(a cycle), or reaches \code{trace_depth} hops.
}
\examples{
adam <- data.frame(
  dataset = c("ADSL", "ADAE"), variable = c("AGE", "AAGE"),
  label = c("Age", "Analysis Age")
)
sdtm <- data.frame(dataset = "DM", variable = "AGE", label = "Age")
map <- data.frame(
  adam_dataset = "ADSL", adam_var = "AGE",
  sdtm_domain = "DM", sdtm_var = "AGE", confidence = 1
)
tm <- build_trace_model(adam, sdtm, mapping = map)
deriv <- data.frame(
  from_dataset = "ADAE", from_var = "AAGE",
  to_dataset = "ADSL", to_var = "AGE"
)
trace_chains(tm, adam_derivations = deriv)

}
