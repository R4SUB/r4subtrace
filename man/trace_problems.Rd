% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_problems.R
\name{trace_problems}
\alias{trace_problems}
\title{The Traceability Problems Worth Acting On}
\usage{
trace_problems(
  trace_model,
  traced_min_level = 2L,
  adam_derivations = NULL,
  trace_depth = 3L
)
}
\arguments{
\item{trace_model}{A \code{trace_model} from \code{\link[=build_trace_model]{build_trace_model()}}.}

\item{traced_min_level}{The minimum trace level that counts as traced, an
integer in \code{0:3}. Default \code{2}.}

\item{adam_derivations}{Optional ADaM-to-ADaM derivation table (see
\code{\link[=trace_chains]{trace_chains()}}). When supplied, chain problems are included.}

\item{trace_depth}{Maximum chain depth to follow, passed to \code{\link[=trace_chains]{trace_chains()}}.}
}
\value{
A tibble with one row per problem: \code{adam_dataset}, \code{adam_var},
\code{problem} (\code{"untraced"}, \code{"ungrounded"}, \code{"cycle"}, or \code{"truncated"}), and a
\code{detail} string. Empty when nothing is wrong.
}
\description{
A full coverage table lists every variable; on a real submission with tens of
thousands of variables that is not what a team consumes. This returns only the
problems: variables that are not traced to the required level, and, when
\code{adam_derivations} is supplied, derivation chains that never reach SDTM, that
are circular, or that were cut off at the depth limit. One variable can appear
more than once when it has more than one kind of problem.
}
\examples{
adam_meta <- data.frame(
  dataset = "ADSL", variable = c("USUBJID", "AGE", "DERIVED1"),
  label = c("Unique Subject ID", "Age", "Derived")
)
sdtm_meta <- data.frame(dataset = "DM", variable = c("USUBJID", "AGE"))
map <- data.frame(
  adam_dataset = "ADSL", adam_var = c("USUBJID", "AGE"),
  sdtm_domain = "DM", sdtm_var = c("USUBJID", "AGE"), confidence = c(1, 1)
)
tm <- build_trace_model(adam_meta, sdtm_meta, mapping = map)
trace_problems(tm)

}
\seealso{
\code{\link[=traceability_coverage]{traceability_coverage()}} for the full per-dataset rollup,
\code{\link[=trace_chains]{trace_chains()}} for the chains themselves.
}
